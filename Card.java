public interface Card                                           //Interface for every card
{
    void playCard(Player user, Player target);                  //Abstract method for each card to overwrite
}